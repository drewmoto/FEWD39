function calcCelciusToFahrenheit (num) {
return (num * (9/5) + 32)
}

function calcFahrenheitToCelcius(num) {
 return (num - 32) * 5/9
}

function calcCircumference (num) {
  return (2 * Math.PI * num)
}

function calcLongestSide (a, b) {
  return (Math.sqrt((a * a) + (b * b)))

}
